port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: false

redis:
  address: localhost:6379

keys:
  devkey: absono-livekit-dev-secret-32-chars!!

logging:
  level: info

webhook:
  api_key: devkey
  urls:
    - http://localhost:8080/api/livekit/webhook
